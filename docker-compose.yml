version: "3"

services:
  db:
    image: mariadb:10
    container_name: jacq-db
    environment:
      MYSQL_DATABASE: ${DB}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    expose:
      - 3306
    ports:
      - 3306:3306
    restart: always
    volumes:
      - ./initdb:/docker-entrypoint-initdb.d
      - db:/var/lib/mysql

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: jacq-phpmyadmin
    environment:
      PMA_ABSOLUTE_URI: https://${DEFAULT_HOST}/phpmyadmin/
    expose:
      - 80
    links:
      - db:mysql_db_server
    restart: always

  wildfly:
    build:
      args:
        WILDFLY_USER: ${WILDFLY_USER}
        WILDFLY_PASSWORD: ${WILDFLY_PASSWORD}
      context: .
      dockerfile: Dockerfile
    container_name: jacq-wildfly
    depends_on:
      - db
    environment:
      VIRTUAL_HOST: ${DEFAULT_HOST}
      VIRTUAL_PORT: 8080
    ports:
      - 9990:9990
      - 9991:9991
    restart: always

  gnparser:
    image: gnames/biodiversity
    container_name: jacq-biodiversity
    expose:
      - 4334
    restart: always

  nginx-proxy:
    image: jwilder/nginx-proxy
    container_name: jacq-nginx-proxy
    depends_on:
      - phpmyadmin
      - wildfly
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/nginx.conf
      - ./nginx/vhost.d:/etc/nginx/vhost.d
      - nginx-html:/usr/share/nginx/html
      - nginx-certs:/etc/nginx/certs:ro
      - /var/run/docker.sock:/tmp/docker.sock:ro
    restart: always

  letsencrypt-nginx-proxy-companion:
    image: jrcs/letsencrypt-nginx-proxy-companion
    container_name: jacq-letsencrypt-nginx-proxy-companion
    depends_on:
      - nginx-proxy
    environment:
      DEFAULT_EMAIL: ${LETSENCRYPT_EMAIL}
      NGINX_PROXY_CONTAINER: jacq-nginx-proxy
    volumes:
      - ./nginx/vhost.d:/etc/nginx/vhost.d
      - nginx-html:/usr/share/nginx/html
      - nginx-certs:/etc/nginx/certs:rw
      - /var/run/docker.sock:/var/run/docker.sock:ro

volumes:
  db:
  nginx-html:
  nginx-certs:
